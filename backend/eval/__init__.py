"""AMT quality-benchmark harness for TheRealBass.

Run `python -m eval.run_eval` or `python eval/run_eval.py` from the backend
directory to score the current transcribe.py against the hand-written
corpus in `eval/corpus/`.
"""
